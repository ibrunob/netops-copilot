-- These databases are created only on first initialization of postgres_data.
-- The application schema is owned by Alembic, not by Compose initialization.
CREATE DATABASE temporal;
CREATE DATABASE temporal_visibility;
CREATE DATABASE keycloak;
