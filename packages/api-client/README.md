# Generated API client boundary

This package will hold the generated TypeScript client for the versioned FastAPI
OpenAPI contract. It is intentionally empty until the persisted case API reaches
Milestone 2.

Do not hand-write a parallel client here. Generate and validate it from the
published contract when that contract is stable.
