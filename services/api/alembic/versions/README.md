# Alembic revisions

This directory intentionally starts empty. The persistence owner must add the
first explicit revision for organization-scoped tables, RLS policy, and audit
contracts together; do not generate an empty revision merely to populate it.
